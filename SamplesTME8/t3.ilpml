class Point extends Object
{
    var x;

    method m1 ()
    self.x;
}

class Point2D extends Point
{
    var y;

    method m1 ()
    (self.y * super);
}

function g(x) (f(x))
function f(x) ("z")

let pc = new Point2D(10, 22) in
	pc has g(2)
